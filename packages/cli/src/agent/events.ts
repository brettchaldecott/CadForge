/**
 * Agent event types — re-exports from shared package.
 * Additional Node-specific event utilities will be added in Phase 2.
 */

export { EventType, type AgentEvent, type TextDeltaEvent, type ToolUseStartEvent, type ToolResultEvent, type CompletionEvent, type ErrorEvent, type PermissionRequestEvent, type ModeChangeEvent } from '@cadforge/shared';
